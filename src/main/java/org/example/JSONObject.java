package org.example;

public class JSONObject {
    private String requestBody;

    public JSONObject(String requestBody) {
        this.requestBody = requestBody;
    }

    public String getString(String name) {
        // Реализация получения строки по имени поля в JSON

        // Пример:
        // Парсинг запроса в JSON объект
        // JSONObject jsonBody = new JSONObject(requestBody);

        // Получение значения конкретного параметра запроса
        // String value = jsonBody.getString(name);

        // Возвращение значения
        // return value;

        // Вам нужно будет реализовать соответствующую логику
        // для доступа к полям в вашей реализации JSONObject.
        return null;
    }
}